<div class="half">
  <h1>Alle temaer</h1>
</div>
<% @themes.each do |theme| %>
  <%= link_to theme_works_path(theme.id) do %>
    <%= image_tag theme.cover_image if theme.cover_image.present? %>
    <%= theme.title %><br>
  <% end %>
<% end %>

